<%- include("../../views/partials/user/header") %>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="custom-card p-5 text-center">
                <div class="success-animation mb-4">
                    <i class="fas fa-check-circle fa-5x text-success"></i>
                </div>
                
                <h1 class="mb-4">Order Placed Successfully!</h1>
                <p class="text-muted mb-4">Thank you for your order. We'll keep you updated on its status.</p>
                
                <div class="order-details text-start p-4 mb-4">
                    <h4 class="mb-3">Order Details</h4>
                    <div class="row mb-2">
                        <div class="col-sm-4">
                            <strong>Order ID:</strong>
                        </div>
                        <div class="col-sm-8">
                            <%= order.orderId %>
                        </div>
                    </div>
                    <div class="row mb-2">
                        <div class="col-sm-4">
                            <strong>Order Date:</strong>
                        </div>
                        <div class="col-sm-8">
                            <%= new Date(order.createdAt).toLocaleString() %>
                        </div>
                    </div>
                    <div class="row mb-2">
                        <div class="col-sm-4">
                            <strong>Payment Method:</strong>
                        </div>
                        <div class="col-sm-8">
                            <%= order.paymentMethod %>
                        </div>
                    </div>
                    <div class="row mb-2">
                        <div class="col-sm-4">
                            <strong>Total Amount:</strong>
                        </div>
                        <div class="col-sm-8">
                            ₹<%= order.finalAmount %>
                        </div>
                    </div>
                </div>

                <div class="shipping-details text-start p-4 mb-4">
                    <h4 class="mb-3">Shipping Address</h4>
                    <p class="mb-0">
                        <%= order.shippingAddress.name %><br>
                        <%= order.shippingAddress.landMark %><br>
                        <%= order.shippingAddress.city %>, 
                        <%= order.shippingAddress.state %> - 
                        <%= order.shippingAddress.pincode %><br>
                        Phone: <%= order.shippingAddress.phone %>
                    </p>
                </div>

                <div class="text-center mt-4">
                    <a href="/orders" class="btn btn-primary me-2">
                        <i class="fas fa-history me-2"></i>View Orders
                    </a>
                    <a href="/shop" class="btn btn-outline-primary">
                        <i class="fas fa-shopping-cart me-2"></i>Continue Shopping
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.custom-card {
    background: #ffffff;
    border-radius: 16px;
    box-shadow: 0 2px 15px rgba(0, 0, 0, 0.05);
}

.success-animation {
    animation: scale-up 0.5s ease-out;
}

.order-details, .shipping-details {
    background-color: #f8f9fa;
    border-radius: 8px;
}

@keyframes scale-up {
    0% {
        transform: scale(0.5);
        opacity: 0;
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}
</style>

<%- include("../../views/partials/user/footer") %>
